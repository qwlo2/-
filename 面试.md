1、浅拷贝，深拷贝

  浅拷贝：在关于堆资源的复制上，只是复制指针并未重新申请内存并复制，源对象和新对象的堆资源是同一份。

  深拷贝：会重新申请内存，并进行堆资源的复制，源对象和新对象的堆资源不是同一份。

2、移动，拷贝构造

   移动构造不是深浅拷贝，它并未申请新内存，而是将源对象的堆资源进行转移，并将源对象置空，只有新对象有着堆资源

   拷贝构造，分为默认与非默认，在默认的情况下是浅拷贝，因此有重复释放的问题；要对其进行重写，改为深拷贝，解决重复释放

3、rule of three/five

  three：指代析构，拷贝构造，拷贝赋值，这里的拷贝通常是深拷贝

  five：指代析构，拷贝构造，拷贝赋值，移动构造，移动赋值，这里的拷贝通常是深拷贝

  现代通常是rule of zero，在关于对资源的管理，使用封装的vector等RAII来代替手动释放

  （用户声明了析构函数后，编译器不会再隐式生成移动构造和移动赋值。）

4、虚表位置

   具有静态存储区（随程序结束而释放，不一定是static），在程序映像的只读数据区（静态数据区，不在堆和栈）

5、vector在使用中需要注意什么（end（）在满内存时指向其他地方，是允许的）

   迭代器和引用的失效情况，在连续插入时，如果是插入在末尾，可能触发扩容，一般是1.5/2倍，此时会重新申请内存，首先考虑移动构造，如果移动构造可能

   抛出异常选择拷贝，此时迭代器失效；插入在中间，会导致插入位置及其之后的所有节点的内存地址改变，迭代器失效。

   可以通过reserve预分配内存，在尾插时就不会触发扩容

   在遍历时删除不能进行++，需要通过erase得到返回的有效迭代器


6、三次握手的过程

  第一次握手，客户端将SYN置为一发送；第二次握手，服务器回复SYN+ACK，此时fd进入半连接队列；第三次握手，同步双方初始序列号，并防止历史连接，
  
客户端收到知道双方可以正常通信，不再将SYN置为1，发送sac,服务器收到后将其放入全连接队列，等待accept处理。
  
7、四次挥手的过程
   关闭连接一般是四次挥手。一方发送 FIN 只表示关闭自己的发送方向，对方 ACK 后仍然可以继续发送数据，因此 TCP 支持 half-close。等另一方也发送 FIN，主动关闭方回复最后 ACK 后进入 TIME_WAIT。

TIME_WAIT会等待2MSL（linux下MSL=30S,标准是60s), 主要用于保证最后一个 ACK 丢失时还能响应重传 FIN，
同时避免旧连接报文影响后续复用相同四元组的新连接。

SO_LINGER 用于控制 socket 执行 close 时的行为。默认关闭 linger 时，close 返回后内核继续发送缓冲区中的数据；如果开启 linger 并设置正的超时时间，close 可能等待一段时间；如果设置为 {1, 0}，则是 abortive close，通常直接发送 RST，未发送数据可能被丢弃。
SO_REUSEADDR 一般在 bind 前设置，主要用于允许服务端在旧连接处于 TIME_WAIT 等情况下更快重新绑定原来的地址和端口。它和 SO_REUSEPORT 不同，后者才主要用于多个监听 socket 共享相同端口。(TIME_WAIT仍然存在，此时会通过匹配四元组区分新旧连接，因此旧连接的 FIN、ACK 等重传报文仍然会由对应的 TIME_WAIT 状态处理，如果四元组相同，此时可以通过seq不匹配来丢弃报文)

8、http，https

  区别在于是否使用了TLS加密，https在建立tcp连接后就进行TLS加密，过程如下：

client向server发送可使用的TLS版本和算法，server接受后选择算法，生成密匙（自己持有私匙），将公匙和证书一起发送给client，

client通过公匙解密，并确认证书是否合法，并为接下来的对称加密生成密匙并发送（之前的过程是非对称加密，有2把密匙；对称是一把），server收到后保存密匙，接下来通过密匙进行加密通话。

9、http各版本的区别

1.1与1.0：
   1、实现了长连接，可以流水线式发送报文，但是存在对头阻塞，相应报文的返回顺序要和请求报文一致。

   2、host头域：区分头一个port的不同主机

   3、range：可以请求部分资源

   4、增加了更多的错误码和缓存策略

1.1和2.0：

  1、二进制分帧，将http报文分为不同帧并有着标识来分辨不同报文，解决了队头阻塞，但是tcp层任然存在队头阻塞

  2、多路复用，tcp的拥塞管理有着慢启动，会限制报文的发送速度，多路复用后即可以一定程度提高速度

  3、头压缩，每个报文都会重复发送同样的请求头，头压缩通过双方维持静态表和动态表来压缩报文，静态表存储着基本信息，包含常见的头部名称，以及特别常见的头部名称与值的组合，动态表存储着双方协商后的信息

  只需要发送需要查询表即可

2.0和3.0：
  1、不再采用tcp，而是QUIC+UDP，解决了tcp层的队头阻塞
  
 2、只需要1RTT来完成连接和TLS（强制）,已经有连接，只需要0RTT

 3、连接迁移，采用标识id来分辨连接，在ip变化时不会断开
        

10、deque在使用中需要注意什么

   一张索引表，每个索引指向固定大小的连续内存块

   deque 也需要扩容，但它不是像 vector 那样重新申请一整块更大的连续内存并搬迁所有元素。deque 通常由一个 map 指针数组管理多个固定大小的 buffer，当头尾 buffer 满时只需要再申请新的 buffer；只有 map 本身放不下更多 buffer 指针时，才会重新分配更大的 map，并迁移这些 buffer 指针，而已有元素所在的数据块通常不会搬迁。因此 deque 的头尾扩展可以保持摊销 O(1)，但 map 重分配可能导致 iterator 失效。


11、超过数组大小的访问会发生什么

   读：由于内存对齐此时可能访问的是自身的地址，也可能是位置地址，数据未知

   写：由于内存对齐此时可能会修改一些无用的空间或者另一个对象的值，会破坏邻接对象、堆管理器元数据，如果访问到了未映射页或保护页，则可能立即触发错误

12、reactor

   有三种经典模型：一是主线程接受并监听连接，就绪后，主线程进行io处理；二是主线程监听并接受后，得到就绪通知并将其投入线程池，有线程池的线程进行io处理；三是主线程只负责监听，接受连接后将其分发到其他线程，其他线程监听并处理io

13、lsm相对B+树优势

  B+树对于修改是原地进行修改，而lsm是将所有的修改转为追加并不进行原地修改，并且将数据的写入和整理转化为后台整理，也就是把前台随机小写聚合成 WAL 顺序追加和后台批量顺序写，因此在密集的写入时，吞吐更好

但是这也带来了更大的写放大，在写入一个kv时可能要写入一个sst，或者触发合并

  除此之外，sst的结构是天然的顺序排列，适合范围查询，mvcc和数据清理；在io读取方面，通过布隆过滤器进行刷选，io次数更少

14、shared-ptr的注意事项

   在构造时尽量使用make-shared，将二次分配转为一次分配；此外只有引用计数才是线程安全，指向的对象并不是线程安全，要用锁等来防止数据

竞争；还有循环引用，当需要2个智能指针相互指向时，使用weak-ptr，防止相互依赖导致无法析构

15、weak-ptr作用

   一是循环引用；二是不具备所有权但有要安全访问对象，如对于异步任务，需要每隔一段时间观察其执行情况，因此可以防止一个监控链表中，但是这里不应该拥有其

所有权，不然会延迟其生命周期，那么使用weak-ptr就可以，对于expired()不是原子的，lock（）才是，因此，直接通过lock获取，然后判断智能智能是否为nullptr

lambda 中如果捕获 this 或裸指针，并且 lambda 会异步执行，就可能出现对象已经析构但回调仍然访问悬空指针的问题。如果对象本身由 shared_ptr 管理，可以捕获 weak_ptr，在回调执行时通过 lock() 临时获取 shared_ptr，对象仍然存在就执行，否则直接退出。
捕获 shared_ptr 也能保证安全，但会延长对象生命周期；捕获 weak_ptr 则不拥有对象。
需要注意，weak_ptr 不能直接解决任意裸指针问题，它必须依赖已有的 shared_ptr 控制块。

16、线程和进程

   在linux实现上，线程和进程都是task类，线程用gid 字段来表示自己所归属的进程 ID。

   进程，线程任务调度的开销主要是切换上下文的开销，线程的开销更小的原因在于，线程共享资源，因此cpu cache/tls等的重复使用率更高

   进程的资源是相互独立的，线程共享进程的堆，全局/静态变量，其他公共资源如文案，但是栈，寄存器，程序计数器独享，

17、当前锁，快照锁，mvcc

   b+树有主键字段（没有主键时才有),回滚指针，事务id这3个隐藏字段，在进行快照读时会生成视图，视图有当前事务id，最小活跃id，未分配的下一个id，活跃

事务id集合，更具每一个语句的id进行判断，如果大于未分配的下一个id或者属于活跃id集合但不是当前事务id，则不可读，否则可以；不可读则根据回滚指针来到

undo log中的上一条（这里的隐藏字段没有主键）。

   当前读指代for updata等加锁读，此时不会根据视图来读，会读到当前满足条件的所有可见版本（commit)，事务提交时解锁。

   幻读在快照度解决，当前读没有。

18、联合索引

   将a，b，c三个作为索引，就是先a，再bc进行排序，因此，要满足最左前缀原则，必须按abc进行查询，不然就是无序，范围查询也是无序

19、怎么让类不在栈中分配

  将析构函数私有，只有普通构造函数，在编译时就会出错，因为无法调用私有函数，进而只能使用堆

20、显式析构编译器不会生成移动

   =default也算，因为此时代表有堆等资源要在析构函数处理，不能简单用默认移动（堆每一个进行move）

21、IPC进程间通信最快的是什么

   是共享内存，它并会将数据从内核态拷贝到用户态

   “通道”是一个比较泛的说法，不一定特指管道；pipe 和 FIFO 都属于进程间通信 IPC 的“管道类”机制。但是下辖这2个

   pipe就是匿名通道，拥有父子进程通过拷贝资源得到；FIFO是命名通道；都是半双工，sockpair才是全双工通道

   单次 write() 如果写入字节数 <= PIPE_BUF，那么多个进程同时往同一个 pipe/FIFO 写时，这次写入具有原子性，不会和其他 writer 的数据交叉。

22、SIGPIPE,EPIPE,RST

   当write时，对方以及关闭会内核会返回信号即sigpipe，异步代表终止进程，通过signal(SIGPIPE, SIG_IGN);进行忽略

   内核会同时返回信号，并设置errno=EPIPE

   RSE代表重置即连接异常

   服务器端发RST：1客户端连接的port不存在，2port存在连接不存在，3SO-LINGER设置为1，0，4关闭连接时，接收缓冲区还有数据

   客户端；1客户端进程异常退出或主动强制关闭，2连接不存在，3客户端机器重启、网络栈状态丢失，4连接被中间设备主动重置。


    



























   
