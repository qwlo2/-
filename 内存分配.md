<img width="300" height="301" alt="image" src="https://github.com/user-attachments/assets/17741d39-5c53-476f-b83a-6f1141911b99" />1、内存空间

<img width="734" height="569" alt="image" src="https://github.com/user-attachments/assets/c19f5703-9d86-4378-994b-bb45bb285df8" />


c++逻辑存储空间：堆，栈，全局/静态，常量

进程虚拟内存空间：内核空间，用户空间

用户空间（内存映射以页为单位，可能存在内存对齐，因此不一定重叠）

栈（start-stack）:主线程，线程栈在mmap中分配（普通局部变量具有自动存储期，未优化时通常由栈帧保存；具体是否真正放在栈上由编译器决定。进入其他函数，栈帧也报错在这里，线程有自己的栈）

mmap：分为映射文件等和匿名映射（共享内存真正共享的是同一批物理页或页缓存中的文件页，可以匿名或非匿名）。
MAP_SHARED->共享内存必备，对文件的修改可被其他看见，并最终写回文件；MAP_PRIVATE->对文件的修改不可被其他看见，不写回。

堆：小内存。

bss：未初始化（初始化为0）的全局/静态，匿名的符号等，（bss只存在于内存）

datda：初始化（非0）全局/静态（内存和可执行文件）

redata：字符串字面量和只读变量，有些被优化了（在text中）

text：代码段

bss,data,redata,tsxt的逻辑大小在运行后基本不变，栈的大小也是，但是地址不是，每次运行时ASLR 会随机化程序装载地址。堆的大小分为3层，以及映射，未映射，不可映射（受程序资源的影响被限制，每种资源有软限制和硬限制，并且可以通过setrlimit对rlimit进行有条件设置）。start-brk，brk，分别代表堆的起始地址，限制已经映射的地址，int brk(void *addr)，将brk扩展到这个地址，失败返回0，errno未ENOMEM；void *sbrk(intptr_t increment)，将brk移动增量（可增可减），成功返回移动之前的brk地址，失败返回(void *)-1。
<img width="300" height="301" alt="image" src="https://github.com/user-attachments/assets/e487b6e1-75e9-4251-8cdc-bc3f73c5fa2f" />

堆的strat-brk，brk在内核刚装载程序时，初始 brk 接近数据段末尾；但真正进入 main() 时，brk 可能已经因为运行库初始化而增长了。
RLIMIT_DATA是heap，data，bss的总大小，当brk到大这里，必须奥调整rlimit才可以增长；RLIMIT_AS是整个虚拟进程空间的极限，即使RLIMIT_DATA可以突破，但超过整个也不行，mmap没有固定的边界，一般是是找一块连续地方，brk碰到mmap的映射也同样回失败。

2、malloc，alloc

   mlloc:
   
  当申请小内存的时，malloc使用sbrk分配内存；（默认分界值是 DEFAULT_MMAP_THRESHOLD，通常是128KB）当申请大内存时，使用mmap函数申请内存；但是这只是分配了虚拟内存，还没有映射到物理内存，当访问申请的内存时，才会因为缺页异常，内核分配物理内存。

  如果每次申请都使用brk，sbrk，mmap等系统调用，会频繁在用户态和内核态切换，因此采用的是内存池的实现方式，一般会直接申请一块大内存在组成内存池。
  
  内存池由bins[128]（128个双向chunk链表，chunk是含有元数据和内存指针）组成，0不用，1做unorsorted_bins，负责充当fast-bins和bins的缓冲区（fast-bins相当于chache，unsorted-bins是TLB，局部性原理）
  
  [2,63)（<512B的），每个链表中的chunk相同，都为inex*8，[64,128)，每个的大小必须符合[index*8，index*8+64)。不大于 max_fast(默认值为 64B)的 chunk 被释放后，首先会被放到 fast bins中，fast bins 中的 chunk 并不改变它的使用标志 P。这样也就无法将它们合并，当需要给用户分配的 chunk 小于或等于 max_fast 时，malloc 首先会在 fast bins 中查找相应的空闲块，然后才会去查找 bins 中的空闲 chunk。
  
   unsorted-bind在2中情况下会被合并入bin；1、当fast-bin和它都没有满足条件的内存时，会将它合并入bins，再找合适的空间。2、在某时，malloc 会遍历 fast bins 中的 chunk，将相邻的空闲 chunk 进行合并，再进入它，随后合并入bins

   当fast bin和bins都不能满足内存需求时，malloc会设法在top chunk中分配一块内存给用户；top chunk（紧挨着brk，是还未分配的大块内存）为在mmap区域分配一块较大的空闲内存模拟sub-heap。（比较大的时候） >top chunk是堆顶的chunk，堆顶指针brk位于top chunk的顶部。移动brk指针，即可扩充top chunk的大小。当top chunk大小超过128k(可配置)时，会触发malloc_trim操作，调用sbrk(-size)将内存归还操作系统。

当chunk足够大，fast bin和bins都不能满足要求，甚至top chunk都不能满足时，malloc会从mmap来直接使用内存映射来将页映射到进程空间，这样的chunk释放时，直接解除映射，归还给操作系统。（极限大的时候）

Last remainder（一个专门服务后续小块请求的“上一次切分剩余块”）是另外一种特殊的chunk，就像top chunk和mmaped chunk一样，不会在任何bins中找到这种chunk。当需要分配一个small chunk,但在small bins中找不到合适的chunk，如果last remainder chunk的大小大于所需要的small chunk大小，last remainder chunk被分裂成两个chunk，其中一个chunk返回给用户，另一个chunk变成新的last remainder chunk。（这个应该是fast bins中也找不到合适的时候，用于极限小的）
<img width="1288" height="957" alt="image" src="https://github.com/user-attachments/assets/bc301652-3190-492b-9626-344ae0102a6e" />

 free：

   1、是否是mmap（A:是否未主分配区  M：由mmap分配（空闲时没有，mmap接触就舒释放当然没有） P：是否在使用）
   
   2、<max_fast，放入fast——bin
   
   3、先与旁边空闲的合并，再看是否和top-chunk合并
   
   4、放入unsorted-bin
   <img width="1289" height="1139" alt="image" src="https://github.com/user-attachments/assets/6d44dbf7-c4d3-4cbc-b58a-8469163d18c5" />
   https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#malloc%E6%98%AF%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E7%9A%84

   
   
3、new和delete（以下存疑）

   基本数据类型，new int ，new int【】，delete都可以删除，它们没有析构函数，只释放内存。
   对象数组必须delete【】，要逐次析构，在数组前有4字节大的数组数量，一次来析构

4、内存屏障（atomic）

   现在大多数现代计算机为了提高性能而采取乱序执行，这可能会导致程序运行不符合我们预期，内存屏障就是一类同步屏障指令，是CPU或者编译器在对内存随机访问的操作中的一个同步点，只有在此点之前的所有读写操作都执行后才可以执行此点之后的操作。
   































   
   

