1、进程和线程的区别

  进程是分配资源的最小单位，会自动分配内存空间，磁盘空间，io设备等。线程是任务调度的最小单位，不会分配资源，各线程共享进程的资源。

  （在linux实现中，都是task任务，都拥有pid，线程通过tgid标志属于哪个进程。底层都通过，do_work创建，只是参数不同。

2、线程调度为什么比进程调度更少开销

任务调度的开销主要来源于上下文求换的开销和cpu chache和TLB缓存未命中的开销，线程由于共享进程的资源，切换上下文后，cpu chache和TLB缓存命中
相对更高，开销更小。

3、线程和进程之前共享那些资源

  共享堆，全局/静态变量，文件等公共资源，独享栈，寄存器，程序计数器

4、进程间通信方式（ipc，6种）

  通道（FIFO（任何进程）,PIPE（父子），）信息队列，共享内存（mmap），unix域套接字（区别于socket API面向网络，这个面向本机，流和报文都可靠）
  信号，信号量（互斥量是保证资源同一时间只有一个访问，信号量保证有序访问，互斥量加/解锁必须同现场，信号量可以一个释放，另一个接受）


https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1%E6%96%B9%E5%BC%8F

5、进程同步

  https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E8%BF%9B%E7%A8%8B%E5%90%8C%E6%AD%A5

6.线程间通信（3种）

   锁（互斥锁，读写锁，自旋锁），posix信号量（，），条件变量

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E7%BA%BF%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1%E7%9A%84%E6%96%B9%E5%BC%8F

7、5种io
  阻塞，非阻塞，信号驱动io，io复用，异步io

阻塞IO:调用者调用了某个函数，等待这个函数返回，期间什么也不做，不停的去检查这个函数有没有返回，必须等这个函数返回才能进行下一步动作

非阻塞IO:非阻塞等待，每隔一段时间就去检测IO事件是否就绪。没有就绪就可以做其他事。非阻塞I/O执行系统调用总是立即返回，不管时间是否已经发生，若时间没有发生，则返回-1，此时可以根据errno区分这两种情况，对于accept，recv和send，事件未发生时，errno通常被设置成eagain

信号驱动IO:linux用套接口进行信号驱动IO，安装一个信号处理函数，进程继续运行并不阻塞，当IO时间就绪，进程收到SIGIO信号。然后处理IO事件。

IO复用:linux用select/poll函数实现IO复用模型，这两个函数也会使进程阻塞，但是和阻塞IO所不同的是这两个函数可以同时阻塞多个IO操作。而且可以同时对多个读操作、写操作的IO函数进行检测。知道有数据可读或可写时，才真正调用IO操作函数

异步IO:linux中，可以调用aio_read函数告诉内核描述字缓冲区指针和缓冲区的大小、文件偏移及通知的方式，然后立即返回，当内核将数据拷贝到缓冲区后，再通知应用程序。

注意：阻塞I/O，非阻塞I/O，信号驱动I/O和I/O复用都是同步I/O。同步I/O指内核向应用程序通知的是就绪事件，比如只通知有客户端连接，要求用户代码自行执行I/O操作，异步I/O是指内核向应用程序通知的是完成事件，比如读取客户端的数据后才通知应用程序，由内核完成I/O操作。

8、事件处理模式
reactor模式（同步非阻塞）中，主线程(I/O处理单元)只负责监听文件描述符上是否有事件发生，有的话立即通知工作线程(逻辑单元 )，读写数据、接受新连接及处理客户请求均在工作线程中完成。通常由同步I/O实现。
（并发模式中的同步和异步

同步指的是程序完全按照代码序列的顺序执行

异步指的是程序的执行需要由系统事件驱动）

半同步半异步模式指io线程异步（可读，可写驱动），工作线程同步

<img width="1608" height="818" alt="image" src="https://github.com/user-attachments/assets/9e57b20b-6460-40f0-9045-0db2c17eead5" />


<img width="1704" height="1011" alt="image" src="https://github.com/user-attachments/assets/d29e8d55-cc95-409b-b9a7-5c2522bd4e7d" />

main-sub reactor是一种架构，one loop one thread 是一种线程/事件循坏的思想


proactor模式（异步非阻塞）中，主线程和内核负责处理读写数据、接受新连接等I/O操作，工作线程仅负责业务逻辑，如处理客户请求。通常由异步I/O实现。
（一般用reactoe实现它）

9、cpu调度的方法（6种）

  先来先服务+最短作业优先+=高响应比优先

   时间片轮转+最高优先级=多级反馈队列

  https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#cpu%E8%B0%83%E5%BA%A6%E7%AE%97%E6%B3%95%E8%BF%9B%E7%A8%8B%E8%B0%83%E5%BA%A6%E7%AE%97%E6%B3%95

10、线程是如何实现的？

   内核线程实现（1：1模型） ，用户线程实现（M：1）模型，混合实现（M:N)模型

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E7%BA%BF%E7%A8%8B%E6%98%AF%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E7%9A%84

11、线程调度（协同式，抢占式)

   协同式:由线程来决定执行时间，并通知系统切换其他线程，实现简单，系统不用考虑同步，缺点是如果阻塞，将一直阻塞
   
   抢占式：由系统来决定线程的执行时间，并且决定执行顺序

12.进程阻塞为什么不占用cpu资源？

   新建态，就绪态，运行态，阻塞态，终止态 挂起态？阻塞时从运行态转为阻塞态，不占cou，等待io，等待结束会进入就绪态，等待cpu

   ET（边缘触发，有点电平触发的感觉），EPOLLIN下，判断通知的条件是，是否发生了状态变化，即从不可读->可读（相当于点评的0->1），此时触发通知
   如果没有读到EAGAEN，即还是可读状态，接受新数据时，没有从不可读到可读的变化，因此不会通知；EPOLLOUT同理要有不可写->可写的变化

   LT（水平触发），判断时是是否可读/可写，只要成立就通知，不需要从0->1的变化

13、死锁（互斥，不可剥夺，持有并等待，循坏等待；互斥是必要条件，因此只能从破环其他3个来避免死锁）

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E6%AD%BB%E9%94%81

14、虚拟内存

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98

15、内存对齐

   1、变量地址必须是自身大小的整数倍数。（char，int，要8字节）

   2、结构体作为变量时，它的大小看作结构体内的最大变量字节（{char，int}，为4字节）。

   3、整个结构体在满足上诉条件后，整个结构体的大小必须是结构体内的最大变量字节的整数倍（int ，char，要是int的整数倍，8字节）

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E8%AF%B4%E4%B8%80%E8%AF%B4%E5%86%85%E5%AD%98%E5%AF%B9%E9%BD%90

16、内存空间的堆和栈的区别是什么？

   1、堆是由用户手动分配回收的，栈是由os自动分配回收的

   2、堆分配的空间更大，栈更小

   3、堆分配的地址从低到高，栈从高到低

   4、堆是由库函数或运算符来分配管理的，效率更低，栈是os管理，由专门的硬件管理地址，分配效率更高

   5、堆分配的空间可能产生内存碎片，栈不会产生
从上到下，分别是栈，mmap区，堆，bss（存储未初始化（初始化为0）的全局/静态变量，只存在于内存），data（初始化（不为0）的全局/静态变量，存在于内存和可执行文件），text代码段
https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4%E7%9A%84%E5%A0%86%E5%92%8C%E6%A0%88%E7%9A%84%E5%8C%BA%E5%88%AB%E6%98%AF%E4%BB%80%E4%B9%88

17、进程虚拟空间是怎么布局的？进程内存模型

<img width="734" height="569" alt="image" src="https://github.com/user-attachments/assets/879f4dda-a0c3-4aef-a867-c5bc6bf1db88" />

18、大/小端字节

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E4%BB%80%E4%B9%88%E6%98%AF%E5%A4%A7%E7%AB%AF%E5%AD%97%E8%8A%82%E4%BB%80%E4%B9%88%E6%98%AF%E5%B0%8F%E7%AB%AF%E5%AD%97%E8%8A%82%E5%A6%82%E4%BD%95%E8%BD%AC%E6%8D%A2%E5%AD%97%E8%8A%82%E5%BA%8F

19.共享内存（posix，mmap，system v三种方式实现，其他2种底层还是mmap）和内存映射

  mmap 是一种虚拟内存映射机制，它把文件、设备或者匿名内存映射到进程的一段虚拟地址空间。调用 mmap 后通常只是建立 VMA 和映射关系，物理页可能在第一次访问发生缺页异常时才真正分配或加载。

共享内存是一种进程间通信方式，它的本质是让多个进程的页表项指向同一批物理内存页。共享内存可以通过 POSIX 的 shm_open + mmap、System V 的 shmget + shmat，或者普通文件的 MAP_SHARED 映射来实现。

因此 mmap 不等于共享内存。MAP_PRIVATE 文件映射和匿名私有映射只属于内存映射，不属于共享内存；只有多个进程以共享方式映射同一个后备对象时，才形成共享内存语义。

https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E5%85%B1%E4%BA%AB%E5%86%85%E5%AD%98%E5%92%8C%E5%86%85%E5%AD%98%E6%98%A0%E5%B0%84%E7%9A%84%E5%8C%BA%E5%88%AB

20、写时拷贝底层原理

   父子进程开始时时共享内存页，只有一方准备修改时，才进行拷贝
https://github.com/guaguaupup/cpp_interview/blob/main/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F.md#%E5%86%99%E6%97%B6%E6%8B%B7%E8%B4%9D%E5%BA%95%E5%B1%82%E5%8E%9F%E7%90%86

21、Linux零拷贝技术

  splice( )函数

tee( )函数






    









































