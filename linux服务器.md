1.socket

int socket(int domain,int type,int protocol);
协议簇（Protocol Family）（int domain）

套接字有许多不同的通信协议分类，由socket函数第一个参数进行传递。其中最重要的是PF_INET（PF_INETH和AF_INET是一个东西，在 Linux 头文件中，它们通常就是别名

#define PF_INET  2
#define AF_INET  PF_INET）

套接字类型（Type）（int type）

socket函数的第二个参数决定套接字数据传输的方式。

协议族并不能决定数据传输方式，因为一个协议族也有很多数据传输方式

面向连接的套接字（SOCK_STREAM）

面向连接的套接字类似于传送带
<img width="224" height="224" alt="image" src="https://github.com/user-attachments/assets/d11947e6-08e4-40ed-8bc1-5efdec1638c2" />

有如下特点

传输的过程中数据不会丢失
按需传送数据
传输数据不存在数据边界问题
两端套接字必须一一对应
收发的套接字内部有缓冲，就是有字节数组。因此通过套接字传输的数据将保存在数组，因此数据可以填满缓冲一次被读取，也可以分段被读取，不存在数据边界问题。当缓冲区占满后，套接字无法接受数据，停止传输。所以不存在数据丢失问题

面向消息的套接字（SOCK_DGRAM）

面向消息的套接字类似于快递

有如下特点

强调快速传送而非顺序传送
传输的数据可能丢失也可能损坏
传输的数据有边界
限制每次传输的数据大小
协议信息（int protocol）

由于socket函数前两个参数的存在，大部分情况可以向第三个参数传递0。但若同一个协议族中存在多个数据传输方式相同的协议，即数据传输方式相同，协议不同，需要第三个参数制定具体协议（存疑，一般根据前2个就可以，

//IPv4中面向连接的套接字
int tcp_socket=socket(PF_INET,SOCK_STREAM,IPPROTO_TCP)
//IPv4中面向消息的套接字
int tcp_socket=socket(PF_INET,SOCK_DGRAM,IPPROTO_UDP)

2、实现基于TCP/IP的客户端服务端

TCP服务端默认函数调用顺序

socket() 创建套接字
bind() 分配套接字地址
listen() 等待连接请求状态（有全连接（三次握手，accept队列）、半连接队列（一次握手），listen的参数规定的是全连接队列的长度
accpet() 允许连接
read()/write() 交换数据（send/rece）
close() 断开连接

TCP客户端默认函数调用顺序

socket() 创建套接字

connect() 请求连接（如果先bind，再connect，也可以，会直接复用端口和ip）

read()/write() 交换数据

close() 断开连接（如果直接关闭，close会把剩余数据发完（linger开启，限定发生时间，10下直接发送RTS报文，也就是没有发送时间），再发fin，但是如果此时有新数据或者有数据未读，会发生RTS报文，因此应该采用shutdown（fd，w),此时会发生FIN，再read直到收到FIN（会超过时间），再关闭，此时就不会再有数据在缓冲区）

实现服务器端重要/必经过程就是给套接字分配IP和端口号（bind），但是客户端实现过程并未出现套接字的分配，而是创建套接字后立即调用了connect函数，为什么？

答：客户端其实是分配了IP和端口号的。在调用connect的时候分配的（何时），在操作系统内核中进行分配（何地），IP用的是主机的IP，端口号随机分配（何种方式）

UDP（sendto，recefrm）

服务器需要bind，服务器的端口要确定不能是临时端口（recefrom），客户端不用bind，sengdto时会临时绑定（与connect一样，可以绑定一个）

write，read需要直到对端，也就是connnect，没有连接只能用sendto，recefrom；UDP也可以连接，流程和TCP一样，不会进行三次连接，它主要是在本地内核中记录：

这个 UDP socket 默认向哪个对端发送
通常只接收该对端发来的数据
可以通过 socket 错误机制获得部分异步错误

connect(udp_socket, ...)

通常不需要对端在线也可能成功，因为它主要修改本机 socket 状态。

https://github.com/guaguaupup/cpp_interview/blob/main/linux%E6%9C%8D%E5%8A%A1%E5%99%A8.md#%E5%AE%9E%E7%8E%B0%E5%9F%BA%E4%BA%8Etcpip%E7%9A%84%E5%AE%A2%E6%88%B7%E7%AB%AF%E6%9C%8D%E5%8A%A1%E7%AB%AF

3、预处理，编译，汇编，链接

预处理（--E）

宏替换
条件编译：根据宏是否定义，决定保留或删除某段代码。
头文件展开
去掉注释
.cpp文件变成了.ii文件（本质上还是.c文件，只不过#include中的程序给链接进去）

编译（--S）（检查语法，模板实例化，优化，生成汇编文件）

gcc调用不同语言的编译器
.ii文件编程.s（汇编文件）
生成汇编文件
模板特例化（声明和定义要放在一起，一般是防止.h）：main.cpp 只看到模板声明，因此可以完成语法检查并生成“调用某个模板特化函数”的未定义符号（于普通函数的没有区别）；模板定义所在的 .cpp 虽然看到了模板函数体，但没有看到具体模板实参，也没有显式实例化，所以不会生成对应特化的机器代码。最终链接器找不到该符号，产生链接错误。

汇编（-c）（汇编指令转为机器指令）

.s文件转化成.o文件
翻译成机器语言指令
二进制文件

链接（每个cpp都会变为.o文件，此时会进行，符号解析（add 到底由谁提供）
重定位（add 地址确定后，调用位置应该填什么值（填入函数的的地址）），合并section（各个cpp的data，bss，redata），引入库。

.o文件变成可执行文件，一般不加后缀。链接其实分为链接.o和库（相当于多个.o），都是先解析符号确定.o文件，然后重定位。

链接阶段有三个对象：普通.o,静态库，动态库。

对于普通.o，进行符号解析确定.o，在进行重定位，重定位的结果时add的地址偏移。

对于静态库，采取静态链接，链接器寻找哪个.o文件提供了所需函数，然后把它加入程序的text部分，然后对它进行普通.o一般处理

对于动态库，采取动态链接，链接器记录一些信息，在重新加载时处理。

4、静态链接和动态链接

静态链接（.a）：链接器从静态库中取出需要的目标代码，复制进可执行文件（也就是和其他.o并列，一起放在text部分）：

特点：

可执行文件较大；
运行时对该动态库依赖较少；
多个程序会各自保存一份库代码；（是多个运行的程序，不是单个cpp文件等）
更新库后，程序通常需要重新链接。

动态链接（.so）：可执行文件通常不包含库的完整实现，只记录：运行时需要某个动态库中的某个符号

特点：

可执行文件较小；
多个进程可以共享动态库的只读代码页；
运行时需要找到对应 .so；
库升级时可能涉及 ABI 兼容问题。

5、库

头文件：函数声明，告诉编译器怎么调用
库文件：函数实现，真正执行功能（把cpp文件编译成.a或者.so，多个.o文件）




















