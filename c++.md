1、内存对齐

{}->这个是一个字节，为了区分每一个结构体，2个空结构体不能同地址

{int d[0]}->在有其他成员下地址是最后一个变量的最后一个字节，如果单独只有它，在不同编译器的大小不同，0/4，在其他地方这个非法

2、数组

int a[3];a表示整个数组对象，不可被赋值，数组对象通常会隐式转换为第一个元素，即使a[0]，因此a。&a[0],&a(此时没有转换）是同一地址

数组指针int (*) []，指针数组 int* b []，整个和int**等价.

因此二维数组int a[][]和int **a不同，只隐式转换一次，因此是int  (*) []和int** 的对比，因此二维数组名不能直接赋值给二级指针。

 3、static:
     分为对变量（局部，全局），对函数，对类。
     
   对变量分为全局变量和局部变量；修饰全局变量变为静态全局变量，限定它在本文件使用；修饰据变量变为静态局部变量，只初始化一次线程安全（第一次失败，第二次可以重新尝试），前面有有一字节来检查其是否初始化，生命周期。
     
   对函数，static修饰后它也限定在本文件使用。
   
   对类分为修饰变量和函数，修饰变量则代表这个变量属于类而不是单个变量，且不能在类内定义，当多个文件同时包含类，链接时会产生重复定义的错误，只能在类外cpp文件定义，不过static const 可以在类内定义（必须要是整数或者枚举量），此时是常量因此没有具体的地址，要取地址还是要在类外定义但不能赋值已经初始化了；现代c++可以用inline+static，inline解决了多重定义的问题或者static+constexpr（隐含了inline），这个是有地址的。修饰函数时，函数属于类而不是对象，因此没有this指针。

https://github.com/guaguaupup/cpp_interview/blob/main/C%2B%2B.md#static-%E5%92%8Cconst%E5%88%86%E5%88%AB%E6%80%8E%E4%B9%88%E7%94%A8%E7%B1%BB%E9%87%8C%E9%9D%A2static%E5%92%8Cconst%E5%8F%AF%E4%BB%A5%E5%90%8C%E6%97%B6%E4%BF%AE%E9%A5%B0%E6%88%90%E5%91%98%E5%87%BD%E6%95%B0%E5%90%97

4、inline

   允许相同定义出现在多个翻译单元中，并保证它们代表同一个函数或变量。

   内联函数，不是强制性，可能造成函数膨胀。

5、extern的作用?

答：当它与"C"一起连用时，如: extern "C" void fun(int a, int b);则告诉编译器在编译fun这个函数名时按着C的规则去翻译相应的函数名而不是C++的；当它作为一个对函数或者全局变量的外部声明，提示编译器遇到此变量或函数时，在其它模块中寻找其定义。

6、explicit的作用？

答：标明类的构造函数是显式的，不能进行隐式转换。

7、constexpr的作用？（constexpr 变量是可在编译期求值的常量对象，它可以有地址；只是不取地址时，编译器常常会把它直接替换为常量值，不实际分配存储）

答：这个关键字明确的告诉编译器应该去验证(函数或变量)在编译期是否就应该是一个常数（这样编译器就可以大胆进行优化）。

8、volatile的作用？

答：跟编译器优化有关，告诉编译器每次操作该变量时一定要从内存中真正取出，而不是使用已经存在寄存器中的备份。

9、mutable的作用？

答：可变的意思，使类中被声明为const的函数可以修改类中的非静态成员.

10、auto和deltype的作用和区别？

答：用于实现类型自动推导，让编译器来操心变量的类型；auto不能用于函数传参和推导数组类型，但deltype可以解决这个问题。

11、define宏常量和const常量的区别

  类型和安全检查不同：defin采取字符替换，没有数据类型，不会进行安全检查，const有数据类型会进行安全检查

  存储方式：define在预处理阶段处理，没有地址，存储与代码段，const是一个变量，有地址，存储在数据段
12、typedf和define（类型采取类型说明符 + 声明符列表，int* a，b，a是int*,b是int）

 作用域：#define没有作用域的限制，只要是之前预定义过的宏，在以后的程序中都可以使用。而typedef有自己的作用域。

 类型和安全检查不同：关键字typedef在编译阶段有效，由于是在编译阶段，因此typedef有类型检查的功能。#define则是宏定义，发生在预处理阶段，也就是编译之前，它只进行简单而机械的字符串替换，而不进行任何检查。

 指针使用：
 
 typedef是别名因此pint（int*）是新类型，const pint p，代表p是常量（修饰指针层），即int* const ；pint a，b，两个都是int*

 define只是字符替换，cosnt pint p->const int *p ;pint a,b，a是int*，b是int

 13、#include<>和#include“ ”的区别

 双引号优先找项目当前目录，再找编译器配置的头文件目录，尖括号主要找编译器和系统配置的头文件目录。因此自己写的必须用“”，因为<>编译器没有配置我们的目录，系统的都可以

 14、std::forward<T>
 
 在函数模板中，完全依照模板的参数的类型（即保持参数的左值、右值特征），将参数传递给函数模板中调用的另外一个函数。

 15、O0、O1、O2、O3优化

 https://github.com/guaguaupup/cpp_interview/blob/main/C%2B%2B.md#o0o1o2o3%E4%BC%98%E5%8C%96

 16、虚函数

   虚函数用来实现多态，虚函数调用只有在指针或者引用时才是运行时确定，值调用时编译期确定

   （1）如果是有虚函数的话，虚函数表的指针始终存放在内存空间的头部；

（2）除了虚函数之外，内存空间会按照类的继承顺序(父类到子类)和字段的声明顺序布局；

（3）如果有多继承，每个包含虚函数的父类都会有自己的虚函数表，并且按照继承顺序布局(虚表指针+字段，有thunk，记录派生类的位置即第一个vptr，通过thunk跳转）；如果子类重写父类虚函数，都会在每一个相应的虚函数表中更新相应地址；如果子类有自己的新定义的虚函数或者非虚成员函数，也会加到第一个虚函数表的后面；

（4）如果有钻石继承，并采用了虚继承，则内存空间排列顺序为：各个父类(包含虚表指针（vbptr）)、子类、公共基类(最上方的父类，包含虚表)，并且各个父类不再拷贝公共基类中的数据成员。

 父类有虚函数就会有vptr；多继承下有thunk确定派生类的虚函数位置（即第一个vptr）；菱形继承下有无虚继承按多几次的格局算，有虚继承时公共基类单独抽出放到最后，先父（vptr，vbptr+字段+thunk）后子（thunk——字段，它的虚函数在第一个vptr）最后基类（字段+thubk）
 
16、虚继承（菱形继承等，有vptr，vbptr虚基表指针）
   虚基类由最派生类（最下面的派生类）直接负责初始化，vbptr紧挨对应的类，记录的是这个类到基类的偏移

   <img width="624" height="447" alt="image" src="https://github.com/user-attachments/assets/fdbf89a6-90d7-4a0a-a2d5-a2b28a4605b2" />

  

17、构造函数可以是虚函数吗？

   虚函数的调用依赖虚表指针，但是调用构造函数之前对象并不存在，也就是没有对应的虚表指针，也就无法调用虚函数。

 18、析构函数可以时虚函数吗？
  为了确保析构的正常进行，父类的析构函数通常是虚函数，只有父类指针虚构时调用的时子类的析构函数不是父类的
   
19、构造和析构中调用虚函数

  构造函数先进行基类的构造此时派生类还未构造，虚表指针指向基类的虚表，调用虚函数会调用基类的，在派生类派生类中调用虚函数，虚表指针指向派生类的虚表，调用的是派生类的版本；
  析构时，虚表指针先指向派生类，调用其虚函数，派生类析构，虚表指针指向基类，调用其虚函数。

20、null和nullptr的区别

   NULL 通常只是整数常量 0 的宏；nullptr（std::nullptr_t只能表示空值不是void*，不能存储地址） 是 C++11 引入的专用空指针字面量。

21、extern 关键字

为了能够正确实现C++代码调用其他C语言代码。加上extern "C"后，会指示编译器这部分代码按C语言的进行编译，而不是C++的。主要原因是C++和C程序编译完成后在目标代码中的命名规则不同。比如C++语言在编译的时候为了解决函数的多态问题，会将函数名和参数联合起来生成一个中间的函数名称，而C语言则不会，因此会造成链接时找不到对应函数的情况，此时C函数就需要用extern “C”进行链接指定，这告诉编译器，请保持我的名称，不要给我生成用于链接的中间函数名。

extern是C/C++语言中表明函数和全局变量作用范围（可见性）的关键字，该关键字告诉编译器，其声明的函数和变量可以在本模块或其它模块中使用。

22、怎样判断两个浮点数是否相等？

bool almostEqual(double a, double b,
                 double relativeEpsilon = 1e-9,
                 double absoluteEpsilon = 1e-12) {
    // 处理相同有限值、相同无穷大、+0.0 和 -0.0
    if (a == b) {
        return true;
    }
    
   // NaN 与任何数都不相等
    if (std::isnan(a) || std::isnan(b)) {
        return false;
    }

   double diff = std::abs(a - b);//abs绝对值
    double scale = std::max(std::abs(a), std::abs(b));

   return diff <= std::max(
        absoluteEpsilon,
        relativeEpsilon * scale
    );
}

真实数值有无限多种
浮点位数是有限的
很多十进制小数无法被二进制精确表示
只能保存最接近的近似值
运算之后还要再次舍入

接近零时看绝对误差，数值较大时看相对误差；误差阈值应由业务精度决定，而不是机械地使用一个固定值。
 

 

















































